CREATE TABLE Representatives (
          RepID int,
          Name varchar(255),
          Party varchar(255),
          District    varchar(255),
          Office varchar(255),
          Phone varchar(255),
          Committees varchar(2555)
);